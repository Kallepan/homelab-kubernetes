apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtualservice-prometheus
spec:
  hosts:
    - prometheus.infra.home.arpa
  gateways:
    - gateway-prometheus
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: kube-prometheus-stack-prometheus.observability.svc.cluster.local
            port:
              number: 9090
